<!DOCTYPE html>
<html>
<head>
  <title>Mychan</title>
  <%= stylesheet_link_tag :all %>
	<%= javascript_include_tag 'jquery-latest.pack' %>
	<%= javascript_include_tag 'thickbox' %>
  <%= csrf_meta_tag %>
</head>
<body>
	<div class="wrapper">
		
	<div class="left-column">
		<br>
		<%=link_to 'На главную', '/'%>
		<ul>
		<%@sections.each do |section|%>
			<li><%=link_to section.name, section%></li>
		<% end %>
		</ul>
	</div>
	
	<div class="right-column">
		<%= yield %>
	</div>
	
	</div>
</body>
</html>
